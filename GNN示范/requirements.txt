pandas
numpy
scikit-learn
matplotlib
networkx
torch_geometric
# CPU version of PyTorch (remove if using GPU specific wheel)
torch
torchvision
torchaudio

# NOTE:
# 如果运行时报 "This probably means that Tcl wasn't installed properly" 或者与 Tkinter 相关的错误：
# 说明系统缺失 Tk (matplotlib 默认交互后端需要)。解决方案：
# 1) 使用 conda:  conda install tk -y
# 2) 或改用非交互后端 (Agg)，代码中已做自动回退。
# 3) 服务器/无界面环境可设置环境变量 MPLBACKEND=Agg